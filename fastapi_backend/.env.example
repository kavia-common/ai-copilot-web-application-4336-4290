# FastAPI Backend Environment

# Required: Your OpenAI API Key
OPENAI_API_KEY=sk-...

# Optional: OpenAI model (defaults to gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Optional: Database URL (defaults to sqlite:///./app.db)
# For Postgres: postgresql+psycopg2://user:pass@host:5432/dbname
DB_URL=sqlite:///./app.db

# Optional: Comma-separated list of allowed CORS origins
# Include your frontend origin(s), e.g., local dev and preview URL.
# Example local dev:
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ORIGINS=http://localhost:3000

# Optional: Port (used when running uvicorn manually)
PORT=8000

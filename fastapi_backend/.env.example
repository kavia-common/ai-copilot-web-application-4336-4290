# FastAPI Backend Environment Variables

# Required for message endpoint
OPENAI_API_KEY=your_openai_api_key_here

# Optional model override (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Database URL (optional). Defaults to local SQLite file sqlite:///./app.db
# Example for Postgres: postgresql+psycopg2://user:password@localhost:5432/mydb
DB_URL=sqlite:///./app.db

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000

# Optional port for local run instructions
PORT=8000

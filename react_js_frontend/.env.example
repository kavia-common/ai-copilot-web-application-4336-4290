# Frontend environment variables

# PUBLIC_INTERFACE
# REACT_APP_BACKEND_URL:
# Base URL for the FastAPI backend. If not set, the app will default to same-origin (window.location.origin).
# Example:
# REACT_APP_BACKEND_URL=http://localhost:8000
REACT_APP_BACKEND_URL=

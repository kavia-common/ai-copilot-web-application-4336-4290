# React Frontend Environment

# Set this if your backend runs on a different origin/port than the frontend.
# Leave unset to use same-origin (window.location.origin).
REACT_APP_BACKEND_URL=http://localhost:8000
